module.exports = {
    plugins: [
        require('autoprefixer')({
            // browsers: [">0%"]
            // "browserslist": ["last 0 version"]
            browsers: ["last 0 version"]
            
            
        })
    ]
}